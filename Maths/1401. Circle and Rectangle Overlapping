link - https://leetcode.com/problems/circle-and-rectangle-overlapping/description/?envType=daily-question&envId=2026-09-19
TC- O(1)
SC- O(1)
code-

class Solution {
public:
    bool checkOverlap(int radius, int xCenter, int yCenter, int x1, int y1, int x2, int y2) {
        // find the closest point -> find the distance point to centre -> it must dist<=radius

        int closestX= max(x1,min(xCenter,x2));
        int closestY= max(y1,min(yCenter,y2));

        int distX=xCenter-closestX;
        int distY=yCenter-closestY;

        return (distX * distX + distY * distY) <= (radius * radius);
    }
};
